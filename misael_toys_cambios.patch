diff --git a/catalogo/views.py b/catalogo/views.py
index a98c5e9..9961a14 100644
--- a/catalogo/views.py
+++ b/catalogo/views.py
@@ -102,6 +102,11 @@ def _contexto_filtros(request):
     }
 
 
+# Ya no es un límite de productos a mostrar: es el tamaño de cada "tanda" que
+# se trae por scroll infinito. El catálogo completo sigue siendo navegable,
+# simplemente se va cargando de a PRODUCTOS_POR_PAGINA en PRODUCTOS_POR_PAGINA
+# a medida que el usuario baja en la página (ver hx-trigger="revealed" en
+# _grid_productos.html).
 PRODUCTOS_POR_PAGINA = 12
 
 
diff --git a/static/css/style.css b/static/css/style.css
index 16c5fa2..0b0aadf 100644
--- a/static/css/style.css
+++ b/static/css/style.css
@@ -15,6 +15,14 @@
 
 * { box-sizing: border-box; }
 
+html {
+  /* Reduce toda la página (tipografía, imágenes, espaciados, bordes) al 90%
+     de su tamaño original de forma proporcional, igual que si el usuario
+     bajara el zoom del navegador al 90%. Soportado en Chrome, Edge, Safari
+     y Firefox 126+. */
+  zoom: 0.9;
+}
+
 body {
   margin: 0;
   background: var(--bg);
@@ -506,23 +514,23 @@ a:hover { color: var(--teal-dark); }
   width: fit-content;
 }
 
-.paginacion {
-  display: flex;
-  justify-content: center;
-  align-items: center;
-  gap: 18px;
-  margin-top: 30px;
-  padding-top: 20px;
-  border-top: 1px solid var(--clay);
-  font-size: 0.9rem;
+/* --- Scroll infinito: se muestra solo mientras se está pidiendo la
+   siguiente tanda de productos (htmx agrega/quita la clase htmx-request) --- */
+.htmx-indicator {
+  display: none;
 }
-
-.paginacion a {
-  font-weight: 600;
+.htmx-request.htmx-indicator,
+.htmx-request .htmx-indicator {
+  display: block;
 }
 
-.paginacion-actual {
+.scroll-cargando {
+  text-align: center;
+  margin-top: 24px;
+  padding-top: 20px;
+  border-top: 1px solid var(--clay);
   color: var(--ink-soft);
+  font-size: 0.9rem;
 }
 
 .sin-resultados {
diff --git a/templates/catalogo/_grid_productos.html b/templates/catalogo/_grid_productos.html
index 0a8f007..a119ea6 100644
--- a/templates/catalogo/_grid_productos.html
+++ b/templates/catalogo/_grid_productos.html
@@ -4,12 +4,19 @@
 </div>
 
 {% if productos %}
-<div class="grid-productos">
+<div class="grid-productos" id="grid-productos">
   {% for producto in productos %}
-  <a href="{{ producto.get_absolute_url }}" class="tarjeta-producto">
+  <a href="{{ producto.get_absolute_url }}" class="tarjeta-producto"
+     {% if forloop.last and productos.has_next %}
+     hx-get="?{{ querystring_sin_pagina }}&page={{ productos.next_page_number }}"
+     hx-trigger="revealed"
+     hx-swap="afterend"
+     hx-select="a.tarjeta-producto"
+     hx-indicator="#scroll-cargando"
+     {% endif %}>
     <div class="tarjeta-imagen">
       {% if producto.imagen_principal %}
-        <img src="{{ producto.imagen_principal }}" alt="{{ producto.nombre }}">
+        <img src="{{ producto.imagen_principal }}" alt="{{ producto.nombre }}" loading="lazy">
       {% else %}
         <span class="sin-imagen">Sin foto aún</span>
       {% endif %}
@@ -24,20 +31,8 @@
   </a>
   {% endfor %}
 </div>
-{% if productos.has_other_pages %}
-<nav class="paginacion">
-  {% if productos.has_previous %}
-  <a href="?{{ querystring_sin_pagina }}&page={{ productos.previous_page_number }}"
-     hx-get="?{{ querystring_sin_pagina }}&page={{ productos.previous_page_number }}"
-     hx-target="#resultado-productos" hx-push-url="true">&larr; Anterior</a>
-  {% endif %}
-  <span class="paginacion-actual">Página {{ productos.number }} de {{ productos.paginator.num_pages }}</span>
-  {% if productos.has_next %}
-  <a href="?{{ querystring_sin_pagina }}&page={{ productos.next_page_number }}"
-     hx-get="?{{ querystring_sin_pagina }}&page={{ productos.next_page_number }}"
-     hx-target="#resultado-productos" hx-push-url="true">Siguiente &rarr;</a>
-  {% endif %}
-</nav>
+{% if productos.has_next %}
+<div id="scroll-cargando" class="scroll-cargando htmx-indicator">Cargando más productos…</div>
 {% endif %}
 {% else %}
 <div class="sin-resultados">No encontramos productos con esos filtros. Prueba quitando alguno.</div>
